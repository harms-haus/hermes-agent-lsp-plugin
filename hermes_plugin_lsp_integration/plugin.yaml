name: lsp-integration
version: "1.1.0"
description: >-
  Auto LSP diagnostics for patch/write_file operations, plus on-demand
  `lsp_diagnostics` tool with force-refresh support. Supports 30 languages.
hooks:
  - transform_tool_result
requires_env: []
